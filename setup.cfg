[metadata]
name = intelope
version = 0.1.0

[options]
py_modules = cli
packages = find:

[options.packages.find]
include = ingestion*, pipeline*, ui*

[options.entry_points]
console_scripts =
    intelope = cli:app
