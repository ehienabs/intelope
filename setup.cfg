[metadata]
name = intelope
version = 0.1.0

[options]
py_modules = cli
packages = find:

[options.packages.find]
include = ingestion*, pipeline*, ui*

[options.package_data]
ui = *.html

[options.entry_points]
console_scripts =
    intelope = cli:app
